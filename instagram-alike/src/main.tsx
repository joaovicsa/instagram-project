/** @format */

import React from 'react'
import ReactDOM from 'react-dom/client'
import Postgram from './routes/postgram.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Postgram />
  </React.StrictMode>
)
